'use client';
import {useState} from 'react';
import Link from 'next/link';
import {useRouter} from 'next/navigation';
import {ArrowRight,ShieldCheck,Eye,EyeOff} from 'lucide-react';
export default function Login(){const router=useRouter();const[error,setError]=useState(''),[busy,setBusy]=useState(false),[show,setShow]=useState(false);return <main className="login"><div className="login-art"><div className="login-orbit orbit-one"/><div className="login-orbit orbit-two"/><div className="login-core">N</div><div className="login-caption"><span className="eyebrow">A LITTLE PEACE OF MIND</span><h1>Your infrastructure.<br/><em>Under watch.</em></h1><p>Every project. Every connection.<br/>One place to see the whole picture.</p></div></div><section className="login-panel"><Link className="brand" href="/">N<span>NightWatch</span></Link><div className="login-form"><ShieldCheck size={28} className="mint"/><h2>Welcome back.</h2><p>Your world is waiting. Sign in to take a look.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');try{const body=new URLSearchParams();body.set('passphrase',String(new FormData(e.currentTarget).get('passphrase')||''));const response=await fetch('/api/session',{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body,signal:AbortSignal.timeout(10000)});const data=await response.json();if(!response.ok)throw Error(data.message);router.replace('/');router.refresh();}catch(e){setError(e instanceof Error?e.message:'Sign in failed');setBusy(false);}}}><label htmlFor="passphrase">Operator passphrase</label><div className="password-field"><input autoFocus id="passphrase" name="passphrase" type={show?'text':'password'} autoComplete="current-password" required maxLength={512}/><button type="button" aria-label={show?'Hide passphrase':'Show passphrase'} onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div>{error&&<p role="alert" className="error">{error}</p>}<button className="button primary login-submit" disabled={busy}>{busy?'Signing in…':'Enter NightWatch'}<ArrowRight size={17}/></button></form><small><ShieldCheck size={13}/> Private access. Credentials stay on the server.</small></div><footer>See more. Keep it running.</footer></section></main>;}
